{
  "name": "logistica-web-functions",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p .",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint ."
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "eslint": "^9.12.0",
    "@types/node": "^20.12.12"
  }
}










